#Declear two variables 'x' and 'y',and assign them integer values.swap the values of those variables without using any temporary variablr

'''x=int(10)
y=int(20)
x=x+y
y=x-y
x=x-y
print("the values of x:",x,"the value of y:",y)

create a program that the area of a rectangle.take the lenght and the width as input from the user and store them in variables
and calculate and display the area

length=float(input('Enter the length of the rectangle :'))
width=float(input('Enter the width of the rectangle :'))
area=length*width
print('The Area of the Rectangle is',area)'''

'''Write a program that converts temperature from celsis to Fahrenhit.take the tempareture in celsius as input and store it in a 
veriable,convert it to farhenheit and display it'''

temp_c=float(input('Enter temperature in Celsius : '))
temp_f=(9/5*(temp_c)+32)
print(f'Temperature in Fahrenheit = {temp_f}')